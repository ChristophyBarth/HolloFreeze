<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HolloFreeze" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/ourPrimaryColor</item>
        <item name="fontFamily">@font/poppins</item>
        <item name="android:background">@color/ourBackgroundColor</item>
        <item name="android:textColor">@color/ourTextColor</item>
    </style>

    <style name="Theme.HolloFreeze" parent="Base.Theme.HolloFreeze" />


</resources>